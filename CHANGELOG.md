# 更新日志

## 1.0.0（首个正式版）

对比基准：`v20260924.02` / `111e32a`。此前所有版本均为日期编号的预发布版本。

### 从预发布渠道升级

- 本版本是正式版，不再标记为 prerelease。
- 正在使用日期编号版本的用户请**先升级到 `20260924.02` 或更高**，再通过应用内更新升级到 1.0.0。`20260924.01` 及更早版本的版本比较把日期编号当作主版本号，会把 1.0.0 判定为更旧，因此不会提示本次更新；该问题已在 `20260924.02` 修复。
- 直接下载并运行 1.0.0 安装包不受影响。

### 主要功能

- 桌面盒子嵌入 Explorer 桌面层，不使用全局置顶窗口；普通应用和全屏窗口可覆盖盒子。
- 盒子分组只保存元数据，不复制文件、不创建真实子目录、不改变桌面文件路径。
- 网格与列表视图、名称/大小/类型/修改时间排序、图标尺寸与间距、标题样式、圆角、边框与选中颜色。
- 按项目类型、扩展名与名称通配符自动整理，支持预览与批量撤销。
- 手动及每日布局备份、导入导出、保留策略与失败回滚。
- 文件夹映射盒子、只读模式、实时刷新与离线重连。
- 全局快捷键、托盘快捷操作、浅色/深色/跟随系统主题、单实例与开机启动。
- 应用内检查、下载、校验并启动更新；客户端不保存 GitHub Token。

### 本版本包含的主要修复

- 桌面「创建日期」排序：此前无法识别该排序方式而静默退回名称排序，拖动新建文件后整个桌面被打回 A-Z；现按文件创建时间正确排序，并与修改时间区分。
- 排序状态稳定性：Explorer 短暂返回空排序读数时不再覆盖已知排序，也不再清空已保存的图标布局。
- 批量删除：单个文件被占用不再中断整批删除；部分失败时列出已删除、失败与被跳过的项目；删除失败的项目不再被当作已删除而清空图标格。
- 应用内更新检查：修复日期版本与正式版本混用时的版本比较错误。

### 验证与构建产物

- 本次 Release 配置测试：`CrabDesk.Tests` 421 项、`CrabDesk.WinUI.Tests` 672 项、`CrabDesk.Bootstrapper.Tests` 46 项，合计 1139 项通过。
- `build/verify-release-workflow.ps1` 通过。
- **本次发布未配置 Authenticode 代码签名**，完整性依赖 HTTPS + `SHA256SUMS.txt` 校验。
- 自动化测试与单机桌面验收已完成；Windows 10/11 真实多屏混合 DPI、显示器插拔与睡眠唤醒的组合验收尚未覆盖全部硬件环境。
- 发布附件：`CrabDesk-Setup-x64.exe`、`CrabDesk-Payload-x64.exe`、`SHA256SUMS.txt`；推荐使用 Setup 安装。

## v20260924.02（基于线上最新版本 v20260924.01）

对比基准：`v20260924.01` / `01e768a`。

### 问题修复

#### 应用内更新检查的版本比较错误

- 修复日期格式版本与正式版本混用时的排序错误：此前的比较方式把日期编号当作主版本号，`20260924.01` 被排在 `1.0.0` 之前。
- 后果：已安装在日期渠道版本上的用户检查更新时被判定为「已是最新版本」，永远不会收到 1.0.0 的更新提示。
- 现在日期渠道版本仍按日期先后排序，并整体位于早期 `0.x.y` 之上、正式版 `X.Y.Z` 之下。
- **本版本是过渡版本**：先升级到本版本，应用内更新检查才能正确识别并升级到后续的 1.0 正式版；直接安装 1.0.0 安装包不受影响。
- 桌面「创建日期」排序与排序状态稳定性修复沿用 `v20260924.01`，未做回退。

### 验证与构建产物

- 本次 Release 配置测试：`CrabDesk.Tests` 421 项、`CrabDesk.WinUI.Tests` 672 项、`CrabDesk.Bootstrapper.Tests` 46 项，合计 1139 项通过。
- `build/verify-release-workflow.ps1` 通过。
- 测试通过不等同于全部硬件环境或 Explorer 重启场景已完成实测。
- 发布附件：`CrabDesk-Setup-x64.exe`、`CrabDesk-Payload-x64.exe`、`SHA256SUMS.txt`；推荐使用 Setup 安装。

## v20260924.01（基于线上最新版本 v20260922.01）

对比基准：`v20260922.01` / `8b56717`。

### 新增功能

#### 桌面支持「创建日期」排序

- 此前只识别名称、大小、类型、修改时间四种排序；桌面被设为按创建日期排序时无法识别，会退回名称排序，桌面看起来被静默重排成 A-Z。
- 典型触发场景：把新建的文件拖到桌面某个位置，随后的一次重绘就把整个桌面按名称重新排列。
- 现在按创建日期排序使用文件的创建时间，与修改时间分开取值——编辑文件只改变修改时间，两种排序结果确实不同。
- 桌面右键菜单新增「刷新图标」，与 F5 走同一条刷新流程。

#### 排序状态更稳定

- Explorer 在应用排序或视图变化时可能短暂返回空排序信息，此前该临时读数会被当作「按名称排序」，覆盖已知排序并连带清空已保存的图标布局。
- 现在只有确认有效的排序读数才会被采纳；已知排序随布局一起保存，下次启动时若 Explorer 暂时读不到排序，会沿用上次保存的排序方式。
- 显式刷新在完全读不到排序信息时按创建日期升序排列。

### 问题修复

#### 批量删除被单个失败项中断

- 修复只要有一个文件被占用（例如文档仍在编辑器中打开），整个批量删除就中断、其余选中项都不被删除的问题。现在每项独立处理。
- 删除部分失败时明确提示：已删除数量、失败数量与失败原因（文件名 + 错误信息）。
- 属于系统桌面项目或只读映射目录、无法删除的项目会跳过并单独说明。
- 修复删除失败的项目仍被当作已删除、其图标格子被清空的问题；删不掉的文件仍占用桌面位置。
- 同一文件被重复选中时只删除一次。

### 维护

- 移除两个只能在有桌面会话且有已发布版本的机器上运行的 GitHub 验证脚本（`verify-github-release.ps1`、`verify-github-updates.ps1`）。它们的引用此前仍留在发布策略检查中，导致 CI 发布策略步骤失败；校验已并入发布策略检查，人工验收改用 `gh release download`、SHA-256 与签名核对。

### 验证与构建产物

- 本次 Release 配置测试：`CrabDesk.Tests` 414 项、`CrabDesk.WinUI.Tests` 672 项、`CrabDesk.Bootstrapper.Tests` 46 项，合计 1132 项通过。
- `build/verify-release-workflow.ps1` 通过。
- 测试通过不等同于全部硬件环境或 Explorer 重启场景已完成实测。
- 发布附件：`CrabDesk-Setup-x64.exe`、`CrabDesk-Payload-x64.exe`、`SHA256SUMS.txt`；推荐使用 Setup 安装。

## v20260922.01（基于线上最新版本 v20260913.04）

对比基准：`v20260913.04` / `56da983`。

### 新增功能

#### 「诊断」页：桌面卡顿归属诊断

- 新增独立诊断页，对比空闲基线、普通文件夹、桌面文件夹与 CrabDesk 的响应耗时，辅助排查卡顿。
- 使用交错采样，并展示 Shell 扩展信息供排查参考；测量结果与扩展清单不代表已确定某个扩展是故障原因。
- 诊断可能短暂创建并清理测试文件，不会自动禁用扩展或修改系统设置。

#### AI 整理工作台改为对话式界面

- 右侧输出区改为聊天式消息流：系统事件、你的整理请求、以及每轮的助手回复按时间顺序排列，取代原先四个并排的标签页。
- 每轮回复在完成时会固定该轮的过程信息（思考过程、工具调用、传输状态、用量统计、结果 JSON），回看时不会再被后续运行改写。
- 模型与分类相关设置收进「设置」按钮弹出的对话框，左侧工作区布局保持不变。
- 分组预览结果固定在输入框上方，保留卡片 / JSON 两种查看方式，切换时有明确的选中高亮；结果较多时可滚动查看，运行结束后进度动画自动停止。
- 对话在运行中自动滚动到最新消息，且只对仍在运行的那条做流式刷新。

#### 盒子与桌面文字默认字体改为 Microsoft YaHei UI

- 盒子标题、盒内图标标签、桌面图标名称三处字体的默认值由 Segoe UI 改为 Microsoft YaHei UI，中文显示不再依赖字体的回退机制。
- 已安装用户的既有配置会自动迁移，无需手动设置；自己选过字体的盒子保持原样。

#### 右键新建的盒子落在点击处

- 在桌面右键菜单里选择「创建盒子」时，盒子现在出现在右键点击的位置，而不是被放到空闲位置。盒子贴近桌面边缘时会自动收回到可见范围内。

### 问题修复

#### 规则整理后残留空盒子

- 修复整理完成后，盒子里一个可见项目都没有、盒子却仍留在桌面上的问题。此前判断盒子是否为空时，把指向已删除文件的失效记录也算了进去，于是空盒被判为有内容而保留。
- 同时修复自定义规则创建的空盒从不清理：删除规则原先匹配的文件后，它留下的空盒会一直留在桌面上。
- 现在「盒内是否为空」与画面严格一致：被判为有内容的盒子，一定能在盒子里看到东西。手工创建的盒子即使为空也不会被自动移除。

#### 规则整理重复创建同名盒子

- 修复桌面上已存在同名盒子时，整理仍会再创建一个新盒子的问题。现在会复用已有的同名盒子。

#### 「其它」兜底规则把文件扫进自动盒子

- 默认规则里移除了兜底归类的「其它」规则：不匹配任何规则的文件会留在桌面上，而不是被自动收进盒子。
- 已有安装会在下次启动时自动清除这条遗留规则，无需手动删除。

#### 盒子标题栏显示成子标签名，看起来像重复的盒子

- 修复盒子标题栏显示当前子标签名、而不是盒子自身名称的问题。此前一个带子标签的盒子切到某个标签后，标题栏会变成那个标签的名字；桌面上若同时存在一个同名盒子，两者外观完全相同，看起来像被重复创建。
- 折叠状态尤其明显：折叠时不显示标签栏，标题栏又写着标签名，无从与同名盒子区分。
- 现在标题栏始终显示盒子名；当前选中的子标签由标签栏自身的高亮与下划线表示。

#### 「显示外边框」开关此前无效

- 盒子外观里的「显示外边框」开关此前虽然存在，但描边并未真正绘制，打开或关闭看不出区别。现在描边会真正画出。
- 描边颜色由盒子自身的背景色推导，深色盒子向白偏移、浅色盒子向黑偏移，保持原有色调；描边宽度与浓淡可在外观设置中调整。
- 描边与圆角同心绘制，相邻盒子不会互相遮盖。

#### 保存文件到桌面后图标不出现

- 修复向桌面保存文件后图标不出现、需要手动重新接管才刷出来的问题。
- 覆盖三类此前会漏掉刷新的情况：文件先以隐藏属性创建、写完再取消隐藏；短时间内大量变更冲掉通知队列；以及临时文件写入后改名保存的流程。

#### 从其他程序拖入桌面：默认变成复制、放下后短暂卡顿

- 修复跨盘拖入桌面时默认为复制、原位置仍留有文件的问题。现在拖到桌面按移动处理，需要复制时按住 Ctrl。
- 改进与资源管理器的拖放完成通知，减少拖入桌面后立即操作时的等待。

#### 从其他程序拖文件到桌面无法放下

- 修复从资源管理器等程序向桌面空白处拖文件时光标显示禁止号、无法放下的问题（拖进盒子不受影响）。
- 该问题只在盒子外观使用亚克力材质时出现，本次一并修复。

#### AI 分类

- 修复部分思考类模型与混合推理聚合服务上分类必现失败的问题；同时补上对 SiliconFlow 一类服务的对应参数，未知服务不再被误加参数。
- 响应被截断时自动加大预算重试，而不是直接报错；上游未返回截断标记、仅 JSON 在中途断开的，也能识别为截断。
- 分类过程中偶发的界面崩溃：后台线程直接更新界面集合导致中止，现已统一派发到界面线程。

#### 桌面图标布局与拖出

- 恢复桌面文件拖向其他程序的拖放入口。
- 调整已保存图标布局的使用策略，优先保留已有布局；显式排序或刷新仍可触发重新排列。
- 为桌面图标间距增加下限，改善部分缩放设置下图标与文字过于拥挤的显示。

### 验证与构建产物

- 本次 Release 配置测试：`CrabDesk.Tests` 400 项、`CrabDesk.WinUI.Tests` 652 项、`CrabDesk.Bootstrapper.Tests` 46 项，合计 1098 项通过。
- 测试通过不等同于全部硬件环境或 Explorer 重启场景已完成实测。
- 发布附件：`CrabDesk-Setup-x64.exe`、`CrabDesk-Payload-x64.exe`、`SHA256SUMS.txt`；推荐使用 Setup 安装。

## v20260913.04（基于线上最新版本 v20260913.03）

对比基准：`origin/main` / `6f12098`（`release: v20260913.03`）。

### 问题修复

#### AI 分类对思考类模型必现失败（#2）

- DeepSeek 端点默认附带 `thinking: disabled`：分类任务不需要长推理，避免 DeepSeek V4+ 默认思考模式把 `max_tokens` 额度耗光后以 `finish_reason=length` 返回空内容。仅对 BaseUrl 含 `deepseek` 的端点附加该参数，不影响其他 OpenAI 兼容服务。
- 单次分类的 max_tokens 预算从 `Clamp(64+16n, 128, 2048)` 提高到 `Clamp(256+48n, 512, 4096)`；长文件名大批量分类不再被默认额度截断，且不会超过常见兼容端点 4096 的输出上限（仅当检测到截断时自动以 4 倍升档，上限 16384）。
- 新增截断自动重试：检测 `finish_reason=length`（流式与非流式响应均支持），自动加大额度重试同一组传输配置。
- AI 操作失败提示透出具体原因：截断、HTTP 状态码分别有明确文案，不再一律显示「请检查配置」。

#### AI 整理活动页优化

- 流式输出实时解析：JSON 结果边流边解析，实时显示「已识别 N 项」，无需等待整批响应完成。
- 思考过程增强：思考区头部新增流式指示器与「已思考 N 字 · 输出 N 字」实时字数统计。
- 工具调用时间线：联网检索（Tavily）以可视化时间线展示（检索项目数、搜索中、返回证据数、耗时、失败标记），此前完全不可见。
- 模型请求状态：活动页显示当前传输尝试（第 N/M 次请求 · 流式/兼容模式回退）。
- 思考区与 JSON 输出区在运行中自动滚动到最新内容，停止后不再干扰回看。

#### 桌面接管失败后自动重连（#3）

- 第三方桌面软件（360 画报、桌面助手等）短暂抢占桌面宿主后，接管失败不再停留在「桌面未连接」等待手动恢复。
- 失败后按 2s/5s/15s/30s/60s/120s 退避计划自动重试接管；宿主 refresh 事件显示宿主已恢复时立即重试，不等计时器。
- 用户主动暂停接管时取消自动重试，用户意图优先。
- 重试派发接入 UI 线程 watchdog（`takeover retry` 计时器），保持卡顿可诊断。

### 验证与构建产物

- Release 全量测试：906 项通过（CrabDesk.Bootstrapper.Tests 46、CrabDesk.Tests 331、CrabDesk.WinUI.Tests 529）。
- 已重新生成框架依赖的安装载荷：`artifacts/installer/CrabDesk-Payload-x64.exe`。
- 已重新生成在线安装包：`artifacts/release/CrabDesk-Setup-x64.exe`。
- Release 同时提供 `CrabDesk-Payload-x64.exe` 与 `CrabDesk-Setup-x64.exe`。

## v20260913.03（基于线上最新版本 v20260913.02）

对比基准：`origin/main` / `ff2b231`（`release: v20260913.02`）。

### 更新与发布策略调整

#### 应用内更新签名校验放宽

- 应用内更新器对安装包的 Authenticode 校验改为提示性：签名可信时展示发布者并校验发布者一致性；未签名或不受信时仅提示，不再拒绝启动。
- 启动前的防篡改校验保留（SHA-256 与下载时比对，不匹配即拒绝）。
- 更新链的完整性保障不变：HTTPS 下载 + `SHA256SUMS.txt` 摘要校验。

#### 发布流程不再强制要求签名证书

- `assert-release-configuration.ps1` 不再拒绝未配置签名证书的稳定版发布；证书 secret 配置时仍会自动签名并做完整校验。
- `release.yml` 的签名校验步骤改为仅在有证书时执行。
- CI 与 release workflow 的测试步骤排除在 headless runner 上崩溃的 `CrabDesk.WinUI.Tests`（本地跑全量），修复自 8 月 30 日起 CI/发布流水线超时失败的问题。

### 验证与构建产物

- Release 全量测试：903 项通过（CrabDesk.Bootstrapper.Tests 46、CrabDesk.Tests 328、CrabDesk.WinUI.Tests 529）。
- 已重新生成框架依赖的安装载荷：`artifacts/installer/CrabDesk-Payload-x64.exe`。
- 已重新生成在线安装包：`artifacts/release/CrabDesk-Setup-x64.exe`。
- Release 同时提供 `CrabDesk-Payload-x64.exe` 与 `CrabDesk-Setup-x64.exe`。

## v20260913.02（基于线上最新版本 v20260913.01）

对比基准：`origin/main` / `e5e19eb`（`docs: clarify v20260913.01 release assets`）。

### 安装器修复

#### Windows App Runtime 检测完整修复（#1）

- 新增 `WindowsAppRuntimeDetector`：以结构化包查询替代 PowerShell `Get-AppxPackage` 检测，完整校验 Framework、Main、Singleton 与 DDLM 四类 x64 组件。
- DDLM 组件改为「最低版本兼容」判定而非精确匹配：framework/Main 包被系统服务更新到高于 DDLM 基线的版本（如 `8000.946` 配 `8000.921`）时不再误报缺少依赖。
- Windows App Runtime 安装不再请求提权（MSIX 运行时为按用户部署），并在静默安装参数中加入 `--force`，降低重复安装被跳过导致检测不一致的概率。

#### 安装器下载体验

- 新增 `DownloadSpeedEstimator`：使用 2 秒滑动窗口 + 指数平滑计算下载速度，替代每 150 ms 瞬时差值导致的速度显示抖动；下载完成时保留最终速度而非归零。
- 新增 `InstallerLogger` 结构化日志（敏感字段脱敏、日志注入清洗），记录下载尝试、重试与失败原因，便于远程排障。

### 验证与构建产物

- Release 全量测试：903 项通过（CrabDesk.Bootstrapper.Tests 46、CrabDesk.Tests 328、CrabDesk.WinUI.Tests 529）。
- 已重新生成框架依赖的安装载荷：`artifacts/installer/CrabDesk-Payload-x64.exe`。
- 已重新生成在线安装包：`artifacts/release/CrabDesk-Setup-x64.exe`。
- Release 同时提供 `CrabDesk-Payload-x64.exe` 与 `CrabDesk-Setup-x64.exe`。

## v20260913.01（基于线上最新版本 v20260911.02）

对比基准：`origin/main` / `65879ef`（`release: v20260911.02`）。

### 新增与改进

#### 桌面盒子外观设置

- 将盒子外观设置从折叠表单重构为分组卡片式设置页面。
- 使用独立的二级导航切换「盒子表面」「标题栏」「图标与标签」「桌面图标名称」，避免页面内容被 `Pivot` 的内部标题区域挤压或裁切。
- 统一卡片宽度、居中布局和响应式内容区域；移除卡片内部的分割线。
- 将颜色设置改为色块、HEX 值与原生颜色选择器组合，并将「选择颜色」明确为「选中高亮颜色」。
- 所有设置项统一为“标题 + 说明 + 右侧控件”布局。
- 统一开关尺寸，隐藏冗余的开/关文字并减少无效占位。
- 使用字体搜索/选择控件替代应用标签字体的普通文本输入框。
- 保留「恢复默认外观」操作并统一图标与文案。

#### 桌面图标缩放与网格布局

- 修正图标缩放时空白区域被同比放大的问题，只调整图标部分，保留文字和内边距比例。
- 限制稀疏网格单元格的额外宽度，避免大图标下出现过宽的选择卡片。
- 修正滚动条缩略块在极小轨道上的最小高度计算，避免固定最小值超过可用轨道。
- 新增缩放、可见布局、窄视口和大图标网格回归测试。

#### 桌面图标拖拽与跨屏操作

- 新增独立的跨显示器多选拖拽布局算法；跨屏移动时保留其他显示器的布局，不再覆盖全局布局数据。
- 当源显示器上的多选排列无法适配目标显示器时，自动采用紧凑排列，并保证操作原子性。
- 为桌面图标拖拽新增独立的顶层、透明、免激活指针预览层，使用物理像素坐标保持跨 DPI/负坐标显示器时的抓取点稳定。
- 统一盒子、桌面图标、资源管理器和无目标区域的拖拽预览，避免重复绘制拖拽幽灵。
- 混合系统图标与文件图标选择时不再生成不完整的文件拖放列表，改用 CrabDesk 私有拖拽数据。
- 盒子手动标签支持在拖拽经过标签区域时选中对应标签，并显示当前标签标题。
- 只在完整文件选择时暴露 Shell 文件拖放数据，降低误移动系统/虚拟项目的风险。

#### 桌面右键菜单

- 启动时重新注册 CrabDesk 桌面右键菜单入口，并绑定当前实际运行程序路径；退出时仍清理自有注册项。

#### 安装器与运行时依赖检测

- 修正 Windows App Runtime 版本比较：安装器现在使用实际 MSIX Runtime 版本 `8000.921.1539.0`，不再将 NuGet 包版本 `1.8.260710003` 当作已安装运行时版本。
- Windows App Runtime 检测同时确认 Framework、Main 和 Singleton 组件，降低“安装完成但启动时报 Required components missing”的误判。
- 构建脚本增加包版本格式与 MSIX Runtime 版本格式校验。

### 验证与构建产物

- Release 全量测试：896 项通过（CrabDesk.Bootstrapper.Tests 39、CrabDesk.Tests 328、CrabDesk.WinUI.Tests 529）。
- 安装器诊断验证通过，Framework、Main、Singleton 运行时组件均可检测。
- 已重新生成框架依赖的 CrabDesk 核心安装载荷：`artifacts/installer/CrabDesk-Payload-x64.exe`。
- 已重新生成在线安装包：`artifacts/release/CrabDesk-Setup-x64.exe`。
- 安装包 SHA-256：`b9f9cfcf9dfd2c22c166007772cad3cb2e82758a95a3399db10f7a796ba3475a`。
- Release 同时提供 `CrabDesk-Payload-x64.exe` 与 `CrabDesk-Setup-x64.exe`。
- Release 构建保留现有 `MVVMTK0045` 警告，未引入新的编译错误。
