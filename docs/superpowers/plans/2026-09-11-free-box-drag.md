# 盒子自由拖拽与近邻吸附（2026-09-11）

- 回退点：`checkpoint/acrylic-overlap-20260911`（提交 `ac2924f`）。
- 实验分支：`codex/free-box-drag`。
- 移除拖动过程中和松手提交时的 4 DIP 位置网格取整；仍按物理像素定位，避免亚像素绘制模糊。
- 工作区边缘仅限制盒子不越界，不再作为磁吸目标，也不显示边缘参考线。
- 只有另一盒子在两个轴向上都距当前盒子不超过 48 DIP，且边/中心线在 8 DIP 内时才吸附。
- 进入吸附时显示蓝色虚线；离开 8 DIP 阈值立即取消吸附与虚线。
- 折叠盒子按当前可见标题栏高度参与近邻判断；跨屏时关闭网格并按目标屏 DPI 保持抓取点。
- 调整大小仍保留原有内容网格尺寸吸附，本次只改变盒子位置拖动。

验证：Core/Runtime 307 项、WinUI 529 项、Bootstrapper 39 项全部通过；Release 构建 0 错误（19 条既有 MVVMTK0045 警告）。测试覆盖自由小数坐标、无盒子/远盒子无吸附、近邻阈值、进入和退出吸附、双轴虚线、边界限制、顺序稳定性、无效输入、跨屏关闭网格，以及真实 DesktopBoxForm 状态更新。
