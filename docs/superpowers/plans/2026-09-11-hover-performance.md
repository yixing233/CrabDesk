# 多盒子快速悬停性能优化（2026-09-11）

- 用户确认自由拖拽版本有效；回退标签：`checkpoint/free-box-drag-20260911`（提交 `5c252d2`）。
- 当前实验分支：`codex/hover-frame-performance`。
- 亚克力盒子在快速切换悬停状态时，完整前景位图提交改为 15ms 定时器合并：同一帧窗口内只提交最后状态。
- 拖拽、框选、调整大小和非亚克力路径保持原来的即时刷新，避免增加交互延迟。
- 盒子自身标题/菜单等小范围悬停反馈仍优先走局部 overlay；盒子展开动画继续由 15ms 动画时钟驱动。
- 相同盒子几何和圆角参数下跳过重复 `SetWindowRgn`，焦点排序变化不会重复修改同一几何区域。
- 参考线的脏区域已经纳入动态渲染范围，快速切换时不会留下虚线残影。

## 验证

- `CrabDesk.Tests`：307 通过。
- `CrabDesk.WinUI.Tests`：530 通过。
- `CrabDesk.Bootstrapper.Tests`：39 通过。
- Release 构建：0 错误；仅保留已有 19 条 MVVMTK0045 警告。
- 快速悬停回归：48 次跨 6 盒子移动消息，完整前景同步绘制 0 次，合并刷新 1 次。

当前实现仍保留现有的盒子展开/收起动画；若同时让大量盒子自动展开，动画帧本身仍会按 15ms 更新，这是必要的视觉状态变化，不会被错误地丢帧。
