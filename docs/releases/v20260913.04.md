# CrabDesk 20260913.04

本版本对比线上最新版本 `v20260913.03`，修复 AI 分类对思考类模型必现失败（#2）与桌面接管断开后不自动重连（#3）两个用户反馈问题。

## AI 分类修复（#2）

- DeepSeek 端点默认附带 `thinking: disabled`，避免思考模式耗尽 `max_tokens` 导致空响应；其他 OpenAI 兼容端点不受影响。
- max_tokens 预算提高至 `Clamp(256+48n, 512, 4096)`，并兼容常见端点输出上限；检测 `finish_reason=length` 后自动以 4 倍额度重试（上限 16384）。
- AI 操作失败提示透出截断或 HTTP 状态码等具体原因。

## AI 整理活动页优化

- 流式输出实时解析，JSON 结果边流边显示「已识别 N 项」。
- 思考区新增流式指示器与实时字数统计。
- 工具调用时间线：联网检索（Tavily）全过程可视化，含检索数量、证据数与耗时。
- 显示当前模型传输尝试（流式 / 兼容模式回退）。
- 思考与 JSON 输出区运行中自动滚动。

## 桌面接管自动重连（#3）

- 接管失败后按 2s/5s/15s/30s/60s/120s 退避自动重试；宿主恢复事件到达时立即重试。
- 用户主动暂停时取消自动重试。
- 重试派发接入 UI 线程 watchdog。

## 验证

- Release 全量测试：906 项通过。
- 发布资产：`CrabDesk-Setup-x64.exe`、`CrabDesk-Payload-x64.exe`、`SHA256SUMS.txt`。
